export enum EventSortField {
    VIEWS = 'views',
    TITLE = 'title',
    CREATED = 'createdAt',
    FEATURE = 'feature'
}

export enum EventType {
    CRYPTO = 'CRYPTO',
    IT = 'IT',
    OTHER = 'OTHER',
}

export enum EventView {
    BIG = 'BIG',
    SMALL = 'SMALL',
}
